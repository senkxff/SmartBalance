<UserControl x:Class="SmartBalance.Views.Controls.PagesControls.IncomePageControls.AdditionalncomeControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SmartBalance.Views.Controls.PagesControls.IncomePageControls"
             mc:Ignorable="d" 
             Height="100" Width="85">
    
    <Grid MouseEnter="Grid_MouseEnter"
          MouseLeave="Grid_MouseLeave">
        <Button Background="Transparent"
                BorderThickness="0"
                Padding="0"
                Focusable="False"
                FocusVisualStyle="{x:Null}" Cursor="Hand">

        <!-- Уборка выделения -->
        <Button.Style>
            <Style TargetType="Button">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <ContentPresenter/>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </Button.Style>

        <Grid Cursor="Hand">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Название -->
                <TextBlock x:Name="Title"
                           Grid.Row="0"
                           Text="Доп. доход"
                           HorizontalAlignment="Center"
                           Foreground="Gray"
                           FontFamily="Comic Sans MS"
                           FontSize="15"/>

                <!-- Круг с картинкой -->
            <Grid Grid.Row="1" 
                  HorizontalAlignment="Center" VerticalAlignment="Center">
                <Ellipse x:Name="Ellipse"
                         Width="55" Height="55"
                         Fill="HotPink"/>

                <Image Source="/Views/Resources/PagesControlsImages/IncomePageControlsImages/AdditionalIncomeControlImage.png"
                       Width="32" Height="32"
                       VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </Grid>

            <!-- Сумма -->
            <TextBlock x:Name="Sum"
                        Grid.Row="2"
                        Text="0"
                        HorizontalAlignment="Center"
                        Foreground="Green"
                        FontFamily="Comic Sans MS" FontSize="15" FontWeight="Bold"/>
        </Grid>
        </Button>
    </Grid>
</UserControl>
