<UserControl x:Class="SmartBalance.Views.Controls.LeftPanelControls.ExpensesControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SmartBalance.Views.Controls.LeftPanelControls"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="220"
             Cursor="Hand"
             ToolTip="Расходы">
    
    <!-- ExpensesControl -->
    <Grid>
        <Grid.RenderTransform>
            <ScaleTransform x:Name="Scale" ScaleX="1" ScaleY="1"/>
        </Grid.RenderTransform>

        <StackPanel Orientation="Horizontal" Margin="5 0 0 0">
            <Image Source="/Views/Resources/LeftPanelControlsImages/ExpensesControlImage.png"
                   HorizontalAlignment="Left"
                   Width="35" Height="35"/>
            <TextBlock Text="расходы"
               Foreground="White"
               VerticalAlignment="Center"
               Margin="10 0 0 0"
               FontFamily="Comic Sans MS"
               FontSize="40"/>
        </StackPanel>
    </Grid>

    <!-- Анимация при наведении -->
    <UserControl.Triggers>
        <EventTrigger RoutedEvent="MouseEnter">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation To="1.05"
                                  Duration="0:0:0.2"
                                  Storyboard.TargetName="Scale"
                                  Storyboard.TargetProperty="ScaleX"/>
                    <DoubleAnimation To="1.1"
                                  Duration="0:0:0.2"
                                  Storyboard.TargetName="Scale"
                                  Storyboard.TargetProperty="ScaleY"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
        <EventTrigger RoutedEvent="MouseLeave">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation To="1"
                                  Duration="0:0:0.2"
                                  Storyboard.TargetName="Scale"
                                  Storyboard.TargetProperty="ScaleX"/>
                    <DoubleAnimation To="1"
                          Duration="0:0:0.2"
                          Storyboard.TargetName="Scale"
                          Storyboard.TargetProperty="ScaleY"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </UserControl.Triggers>

</UserControl>
